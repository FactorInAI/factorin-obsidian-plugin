{
	"name": "@repo/shared",
	"version": "0.0.1",
	"private": true,
	"license": "MIT",
	"type": "module",
	"exports": {
		"./*": {
			"default": "./src/*.ts",
			"types": "./src/*.ts"
		}
	},
	"scripts": {
		"fix": "oxlint --silent --fix --type-aware && oxfmt",
		"check": "tsc && oxlint --type-aware && oxfmt --check",
		"tests": "bun test"
	}
}
