{
	"name": "@repo/shared",
	"version": "0.0.1",
	"private": true,
	"license": "MIT",
	"type": "module",
	"exports": {
		"./*": "./src/*.ts"
	},
	"scripts": {
		"lint": "oxlint --fix --silent --type-aware && oxfmt",
		"check": "tsc && oxlint --format agent --type-aware && oxfmt --check",
		"tests": "bun test"
	}
}
