name: Release SDK

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  release-sdk:
    runs-on: ubuntu-slim

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install

      - name: Build SDK
        run: bun run build -F @hesprs/sync-engine-sdk

      - name: Publish to npm
        run: cd packages/plugin && bun publish
